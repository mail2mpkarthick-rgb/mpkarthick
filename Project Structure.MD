g_automation_ai/
├── app/
│   ├── __init__.py          # Marks 'app' as an importable Python package
│   ├── agent.py             # Ollama LLM Core (Handles script generation & prompt tuning)
│   ├── database.py          # SQLite context & SQLAlchemy schemas for healing history
│   ├── executor.py          # Subprocess wrapper for 'npx playwright test' execution
│   ├── generator.py         # Handles writing and reading files inside the workspace
│   ├── healer.py            # Error parser that builds recovery prompts for Ollama
│   ├── main.py              # FastAPI core router exposing API endpoints
│   └── parser.py            # Multi-format document parser (PDF, Word, OCR Images)
│
├── workspace/               # Isolated execution sandbox environment
│   ├── package-lock.json    # Pinpoint version lock for Node modules
│   ├── package.json         # Node configuration, scripts, and runtime dependencies
│   ├── playwright.config.js # Core execution rules, browser profiles, and failure tracking
│   │
│   ├── pages/               # Storage path for generated Page Object Models (POM)
│   │
│   ├── reports/             # Output path for test execution statistics
│   │   ├── dashboard.html   # Visual front-end HUD interface
│   │   └── test-results.json# Raw test report metadata compiled by Playwright
│   │
│   ├── test-results/        # Auto-generated by Playwright on runtime failures
│   │   └── [test_name]/     # Isolated failure artifact folder per test
│   │       ├── trace.zip    # Full browser execution trace timeline
│   │       ├── video.webm   # Live screen recording of the crash
│   │       └── failure.png  # High-res visual snapshot for the vision model
│   │
│   └── tests/               # Runnable test suites generated by the AI
│       └── example.spec.js  # Sample dynamic automation execution script
│
├── healing_memory.db        # Auto-generated SQLite binary database file
└── requirements.txt         # Consolidated Python ecosystem package dependencies
